on:
  schedule:
  - cron: '16 11 * * *'
  push:

name: Excavator
jobs:
  excavate:
    name: Excavate
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Excavate
      uses: shovel-org/GithubActions@1.5.0.6
      env:
        GITH_EMAIL: lacvtg.a1.2023@gmail.com
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SKIP_UPDATED: '1'
        SCOOP_BRANCH: main
